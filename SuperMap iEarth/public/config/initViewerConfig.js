const initViewerConfig = {
    //硬件反走样，1-8，默认值为1
    msaaLevel: 4,
    requestWebgl2: true,
    maxDrawingBufferWidth: 2560,
    maxDrawingBufferHeight: 1440,
}

window.initViewerConfig = initViewerConfig;